# Base URL of the FastAPI backend (AgentCore-orchestrated)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Toggle mock-mode (true = use generated mock data when backend/AgentCore is unavailable)
NEXT_PUBLIC_USE_MOCKS=False
