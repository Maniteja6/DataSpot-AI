# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.9.2
pydantic-settings==2.6.1

# --- Data processing ---
pandas==2.2.3
numpy==1.26.4
pyarrow==18.0.0
duckdb==1.1.3
openpyxl==3.1.5

# --- ML / forecasting ---
scikit-learn==1.5.2
statsmodels==0.14.4
# Prophet is optional — heavier native deps. forecasting_service.py falls
# back to statsmodels Holt-Winters automatically if Prophet isn't installed.
# prophet==1.1.6

# --- Orchestration (LangGraph / LangChain) ---
langgraph==0.2.53
langchain-core==0.3.15

# --- AWS ---
boto3==1.35.68

# --- Export generation ---
reportlab==4.2.5
python-pptx==1.0.2

# --- RAG / vector store ---
# faiss-cpu is optional — local_faiss_store.py falls back to a pure-numpy
# cosine-similarity index automatically if it isn't installed.
# faiss-cpu==1.9.0

# --- Testing ---
pytest==8.3.3
httpx==0.27.2

# --- Utilities ---
python-dotenv==1.0.1
tenacity==9.0.0
